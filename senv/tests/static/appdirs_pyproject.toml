[tool.poetry]
name = "appdirs"
version = "1.1.4"
authors = ["Jorge <jirazabal@flatiron.com>"]

[tool.poetry.dependencies]
# fake dependency to test package lock skipping this dep
# app lock should get the dependencies form the project name + version
click = "*"

[tool.senv]
conda-channels = [
"conda-forge"
]

